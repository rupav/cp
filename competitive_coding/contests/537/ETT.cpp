/// Euler Tree and its applications!
#include<bits/stdc++.h>
using namespace std;

#define fr(i,n) for(int i=0; i<n; i++)
#define rep(i, st, en) for(int i=st; i<=en; i++)
typedef long long ll;
typedef pair<int, int> pii;
ll mod = 1e9+7;


int main(){
    int n;  /// no. of vertices (1 to n)
    int a, b;  /// edges connections. - (n-1) for a tree.
    cin>>n;
    fr(i, n-1){
        cin>>a>>b;  /// root is not defined, only connections.
    }
    return 0;
}

